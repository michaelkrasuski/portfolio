Static assets directory

Place images, icons, and other static content here. Recommended subfolders:

- `images/` — UI images, screenshots
- `icons/` — SVG/icon assets
- `photos/` — profile or project photos (placeholders until replaced)

When referencing assets from `index.html`, use relative paths like `static/images/your-image.png`.
